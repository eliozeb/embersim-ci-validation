# EmberSim generated workspace — regenerated by CI on every run
ember_sim/

# Build artifacts
*.o
*.exe

# Generated trace output (golden baseline lives in .embersim/baseline/)
trace.jsonl

# Rust build artifacts from checked-out EmberSim (CI only)
.embersim/target/
