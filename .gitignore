# EmberSim generated workspace — regenerated by CI on every run
ember_sim/

# Build artifacts
*.o
*.exe

# Generated trace output (golden baseline is committed at baseline/trace.jsonl)
trace.jsonl

# EmberSim tool checkout — CI only, never committed
_embersim/
